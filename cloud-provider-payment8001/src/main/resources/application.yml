server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    # type: com.alibaba.druid.pool.DruidDataSource # 当前数据源操作类型
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_cloud2020?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true&serverTimezone=UTC
    username: "root"
    password: "0824"

mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.cc.entity # 指定entity别名类的路径
